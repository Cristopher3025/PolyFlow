PolyFlow E2E functional on Windows: BASIC-256 + Fortran + COBOL + MIPS

- BASIC-256 stage rewritten for v0.9.6.68 (no map/for-each/instrx,
  bracket arrays, -r flag; output verified byte-identical).
- Fortran: static linking + toolchain PATH auto-prepend (fixes
  0xC0000139 crash) and VIENTO_MAX metric per the course statement.
- COBOL rules engine implements the official grammar
  (identificador operador numero) with TEST-NUMVAL validation.
- MIPS checksum implements the course formula (10/20/30/40,
  checksum + value XOR position); orphaned block repaired.
- Orchestrators use the official messages and file names
  (datos_normalizados, metricas, alertas, secuencia, reglas);
  FINDSTR /X LF-only validation fixed via CRLF normalization.
- MARS 4.5 vendored in tools/ with .gitignore exception; auto-detected
  by scripts/config.bat (original Missouri State site is offline).
- Docs synced: GRAMMAR, CHECKSUM, DATA_CONTRACT, contrato_datos,
  gramatica, arquitectura, stage READMEs and TROUBLESHOOTING (12 sections).
